name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'
          extensions: mbstring, xml
          coverage: none
      - name: Install dependencies
        run: composer install --no-interaction --prefer-dist
      - name: Run tests
        run: ./vendor/bin/pest

  release:
    needs: tests
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - name: Create Release
        run: |
          # Generate notes and append Packagist link
          NOTES=$(gh api repos/${{ github.repository }}/releases/generate-notes \
            -f tag_name=${{ github.ref_name }} \
            --jq '.body')

          gh release create ${{ github.ref_name }} --notes "${NOTES}

          ---

          ## Install

          \`\`\`bash
          composer require fgilio/agent-skill-foundation
          \`\`\`

          📦 [View on Packagist](https://packagist.org/packages/fgilio/agent-skill-foundation)"
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
